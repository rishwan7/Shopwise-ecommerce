<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Category</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <style>
        .bg-custom {
            background-image: url(/assets/ecommerce\ background.jpg); /* Replace with your image path */
            background-size: cover;
            background-position: center;
        }
    </style>
</head>
<body class="bg-gray-100">
    <div class="header flex justify-between items-center p-4 bg-white shadow-md">
        <div class="logo">
            <img src="../assets/shopmate logo.png" alt="Company Logo" class="h-10">
        </div>
        <div class="nav-box space-x-4">
            <a href="/admin" class="btn bg-blue-500 text-white py-2 px-4 rounded hover:bg-blue-600">Back to Home</a>
            <a href="/admin/logout" class="btn bg-red-500 text-white py-2 px-4 rounded hover:bg-red-600">Logout</a>
        </div>
    </div>

    <div class="main-content flex mt-8">
        <div class="sidebar w-1/4 p-4 bg-white shadow-md">
            <a href="/admin/addproduct" class="btn block bg-blue-500 text-white py-2 px-4 rounded mb-2 hover:bg-blue-600">Add Product</a>
            <a href="/admin/viewproducts" class="btn block bg-blue-500 text-white py-2 px-4 rounded mb-2 hover:bg-blue-600">View Products</a>
            <a href='/admin/viewcategories' class="btn block bg-blue-500 text-white py-2 px-4 rounded mb-2 hover:bg-blue-600">View Categories</a>
        </div>

        <div class="container w-3/4 p-8 bg-white shadow-md">
            <h1 class="text-2xl font-bold mb-6">Admin Dashboard</h1>

            <section>
                <h2 class="text-xl font-semibold mb-4">Add New Category</h2>
                <form action="/admin/addcategory" method="POST">
                    <div class="form-group mb-4">
                        <% if (sucessmessage) { %>
                          <p class="text-green-500"> <%= sucessmessage %></p> 
                        <% } %>

                        <% if (errorMessage) { %>
                            <p class="text-red-500"><%= errorMessage %></p>
                        <% } %>

                        <label for="categoryName" class="block text-sm font-medium text-gray-700 mb-1">Category Name:</label>
                        <input type="text" id="categoryName" name="categoryName" class="w-full px-3 py-2 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-blue-500">
                    </div>
                    <div class="form-group mb-4">
                        <label for="newSubcategory" class="block text-sm font-medium text-gray-700 mb-1">New Subcategory:</label>
                        <input type="text" id="newSubcategory" name="subcategoryName" class="w-full px-3 py-2 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-blue-500">
                    </div>
                    <button type="submit" class="btn bg-green-500 text-white py-2 px-4 rounded hover:bg-green-600" id="sweetalert">Add Category</button>
                </form>
            </section>
        </div>
    </div>
</body>
</html>
